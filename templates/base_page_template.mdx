---
title: "(METHOD) (ROUTE)"
description: (DESCRIPTION)
sidebar_label: (SHORT)
slug: (SLUG)
sidebar_position: (SIDEBAR_POSITION)
---
{/*
Any valid mdx can go in this file. So note, that which rendering library you use
may enable you to render all kinds of fancy things. These have been omitted so
that this repository is platform agnostic.

import Highlight from "@site/src/components/highlight.js"

# <Highlight color="(METHOD_COLOUR)">(METHOD)</Highlight> `(ROUTE)`

*/}

(METHOD) `(ROUTE)`

(DESCRIPTION)

(PRIVILEGED)
:::info Note

This is a privileged endpoint. Only authorised queries will be processed.

:::
(/PRIVILEGED)

(PARAMETERS)

## Examples
{/* 

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

*/}

(FOOTER)
